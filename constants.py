from colors import *

WIDTH, HEIGHT = 1920, 1080
ratio = 3 / 5
WIDTH, HEIGHT = int(WIDTH * ratio), int(HEIGHT * ratio)

# game
FPS = 60
BALL_SIZE = 50
PLAYER_SIZE = (BALL_SIZE, 2 * BALL_SIZE)
